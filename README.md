# x-platform-proto
Managing Protobuf Files and Auto-Generating Java Models with GitHub Actions

## Build model
```shell
mvn clean install -P[profile-name]
#example mvn clean install -Puser-manager
```
### Default build all model 
```shell
mvn clean install
```